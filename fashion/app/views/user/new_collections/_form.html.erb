<%= form_for(new_collection) do |f| %>
  <% if new_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(new_collection.errors.count, "error") %> prohibited this new_collection from being saved:</h2>

      <ul>
      <% new_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
